'use client';

import AuthForm  from '@/components/auth/AuthForm';

export default function LoginPage() {
  return (
    <div className="relative flex justify-center w-full items-center h-screen overflow-hidden">
        <AuthForm />
    </div>
  );
}
